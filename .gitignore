# cache files for sublime text
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache

# workspace files are user-specific
*.sublime-workspace

# project files should be checked into the repository, unless a significant
# proportion of contributors will probably not be using SublimeText
*.sublime-project
*.cache

# sftp configuration file
sftp-config.json

# ignore generated binary .DS_STORE
.DS_STORE
.DS_Store

# ignore node modules. They can be added via npm install
node_modules

# ignore the generated coverage information
coverage